<p id="notice"><%= notice %></p>

<h1>Companies</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
      <th>Cantidad de empleados/compañia</th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
    <tr>
      <td><%= link_to company.name, company%></td>
      <td><%= link_to 'Show', company %></td>
      <td><%= link_to 'Edit', edit_company_path(company) %></td>
      <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <td class="compañia_empleado"><%= company.employees.count %></td>
    </tr>
    <% end %>
    El total de Compañias es: <%= @companies.count %>

    <table>
      <thead>
        <tr>
          <th>Nombre Area</th>
          <th> Cantidad de empleado/area</th>
        </tr>
      </thead>

      <tbody>
    <% @area.each do |area| %>
    <tr>
    <td><%= area.name %></td>
    <td class="area_empleado"><%= area.employees.count %></td>
  </tr>
    <% end %>

  </tbody>
</table>

<br>

<%= link_to 'New Company', new_company_path %>
